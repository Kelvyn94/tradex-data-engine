# Core
python-dotenv
# pydantic: transitive dependency of fastapi itself, even though this
# app's own code never imports it directly - confirmed by removing it
# and watching FastAPI fail to import. pydantic-settings, which this
# app genuinely doesn't use (Settings is a plain dataclass), stays removed.
pydantic
sqlalchemy
psycopg2-binary
fastapi
uvicorn[standard]
websockets

# Data Processing
pandas
numpy
scipy

# Providers
yfinance
requests

# Scheduling
apscheduler
pytz
